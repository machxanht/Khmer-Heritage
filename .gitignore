# Dependencies
node_modules/

# Builds & caches
.expo/
dist/
web-build/
*.tsbuildinfo

# IDE / OS
.DS_Store

# Secrets — HARD RULE: never commit credentials (docs/LICENSING.md, PROJECT_SPEC §4)
.env
.env.*
!.env.example

# Licensing evidence contains personal data — tracked separately by owner, not in git
# (ledger itself IS tracked; evidence PDFs/emails are NOT)
/licenses/evidence/
