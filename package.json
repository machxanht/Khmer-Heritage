{
  "name": "khmer-heritage",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "packages/*",
    "app"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "seed": "node scripts/dev-server.mjs",
    "sitemap": "node scripts/generate-sitemap.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
